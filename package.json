{
  "name": "terraform-plan-inspector",
  "version": "1.0.0",
  "description": "GitHub Action to analyze Terraform plan diffs",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format cjs",
    "package": "bun run build && cp action.yml dist/",
    "test": "bun test"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "keywords": ["terraform", "github-action", "diff", "analysis"],
  "author": "",
  "license": "MIT"
}